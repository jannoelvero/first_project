[project]
name = "first-project"
version = "0.1.0"
description = "Data analytics project"
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "numpy>=2.0.2",
    "pandas>=2.2.3",
    "matplotlib>=3.9.2",
    "seaborn>=0.13.2",
    "jupyter>=1.1.1",
    "ipykernel>=6.29.5",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "project_template",
]
